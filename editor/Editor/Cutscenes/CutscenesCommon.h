#pragma once

namespace GuGu{
    /*
        一些常用变量和枚举
    */

    //标尺高度
    constexpr float gRuleHeight = 24.0f;

    //轨道高度
    constexpr float gTrackHeight = 28.0f;

    //标签列的宽度
    constexpr float gLabelWidth = 20.0f;

    //关键帧图标大小(半径)
    constexpr float gKeyDiamondR = 5.0f;
}