#include <pch.h>

#include "AndroidInput.h"

namespace GuGu{

    uint32_t AndroidInput::getKeyMap(uint32_t *keyCodes, GuGuUtf8Str *keyNames, uint32_t maxMappings) {
        return 0;
    }

    uint32_t AndroidInput::getCharKeyMap(uint32_t *keyCodes, GuGuUtf8Str *keyNames, uint32_t maxMappings) {
        return 0;
    }
}