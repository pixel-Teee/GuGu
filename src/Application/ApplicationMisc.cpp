#include <pch.h>

#include "ApplicationMisc.h"

namespace GuGu {
	void ApplicationMisc::ClipboardCopy(const GuGuUtf8Str& str)
	{
	}
	void ApplicationMisc::ClipboardPaste(GuGuUtf8Str& str)
	{
	}

	void ApplicationMisc::getSaveFilePathAndFileName(std::shared_ptr<WindowWidget> ownerWindow, const GuGuUtf8Str& initDir, GuGuUtf8Str& filePath, GuGuUtf8Str& fileName, const std::vector<GuGuUtf8Str>& filterArray)
	{

	}

	void ApplicationMisc::getOpenFilePathAndFileName(std::shared_ptr<WindowWidget> ownerWindow, const GuGuUtf8Str& initDir, GuGuUtf8Str& filePath, GuGuUtf8Str& fileName, const std::vector<GuGuUtf8Str>& filterArray)
	{
	}
}